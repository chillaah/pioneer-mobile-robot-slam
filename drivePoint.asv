function [lWv, rWv] = drivePoint(q, GOAL)
    
% constants
Kp = 1;
Kv = 1;

v = Kv*sqrt((GOAL(1) - q(1))^2 + (GOAL(2) - q(2))^2);
% goal heading
theta = atan(GOAL(2) - q(2), GOAL(1) - q(1));

% heading angle error
e = Kh*(theta - q(3));



end